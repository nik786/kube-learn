[volumes]
Internal storage of a container is ephemeral which means it is designed to be temporary
Volumes allows to provide more permanent storage to a pod which exist beyond the life of a container

[EmptyDir]

It creates storage on a node when pod is assigned to the node.
The storage disappear when the pod leaves the node.


